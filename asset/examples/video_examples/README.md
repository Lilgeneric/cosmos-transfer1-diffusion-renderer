# Local Video Examples

Place local input videos in this directory when running the upstream video
example commands. MP4 files are intentionally ignored by git to avoid committing
large local datasets.
